# Infrastacture as a code tasks (Terraform / CloudFormation)

File vpc.tf - configuration template for VPC created in 6_AWS

Terraform commands:

`terraform init`

`terraform plan`

`terraform apply`

`terraform destroy`